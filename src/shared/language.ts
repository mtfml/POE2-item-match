// Replaces EE2's AppConfig().language for the ported parser - single fixed
// language for this app rather than a full settings system. Edit LANGUAGE to
// change (must match a directory under data/, e.g. data/de/).
export const LANGUAGE: string = "en";

// Your trade-site account name (shown on your own listings, e.g.
// "YourName#1234"). Set this to highlight your own listings in search
// results instead of confusing them for someone else's sale. Leave "" to
// disable.
export const ACCOUNT_NAME: string = "miniotaur#6021";
